The files inside executable-handler are the source codes for the application that runs the program sent by users. All these files will be used inside the docker container.

Requirement: https://github.com/nlohmann/json/tree/develop/src
